[tox]
skipsdist = true
envlist = py312

[gh-actions]
python =
    3.12: py312

[testenv]
passenv = PYTHON_VERSION
allowlist_externals = uv
commands =
    uv sync --frozen --python {envpython}
    uv run python -m pytest tests --cov --cov-config=pyproject.toml --cov-report=xml
    mypy
